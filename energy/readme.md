# DRL Model and Server

This folder holds the DRL model code in Python along with a simple inference server that uses Flask. In case of deploying this project to production, we recommend using WSGI server instead.

